version: "2"
overrides:
  go:
    overrides:
      - db_type: "vector"
        nullable: true
        engine: "postgresql"
        go_type:
          type: "Vector"
sql:
  - engine: "postgresql"
    queries: "./db/sql/query.sql"
    schema: "./db/sql/schema.sql"
    gen:
      go:
        package: "db"
        out: "db"